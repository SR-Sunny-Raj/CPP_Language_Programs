#include <bits/stdc++.h>
using namespace std;
int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int N, val;
        cin >> N;
        val = sqrt(N);
        cout << val << endl;
    }
    return 0;
}