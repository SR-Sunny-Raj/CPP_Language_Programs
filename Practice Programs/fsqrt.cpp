#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int x;
        cin >> x;
        cout << int(sqrt(x)) << endl;
    }
    return 0;
}